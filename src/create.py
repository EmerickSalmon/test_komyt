```python
def create_vm(name, template, datacenter, cluster, resource_pool):
    # Placeholder function to simulate VM creation
    print(f"Creating VM '{name}' from template '{template}' in {datacenter}/{cluster}/{resource_pool}")
```
